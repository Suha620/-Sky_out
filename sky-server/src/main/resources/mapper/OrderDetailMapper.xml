<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">
    <insert id="insertBatch">
        insert into order_detail (name, image, order_id, dish_id, setmeal_id, dish_flavor,number, amount) values
        <foreach collection="list" separator="," item="orderDetail" >(
            #{orderDetail.name},#{orderDetail.image},#{orderDetail.orderId},
            #{orderDetail.dishId},#{orderDetail.setmealId},#{orderDetail.dishFlavor},#{orderDetail.number},
            #{orderDetail.amount}
            )
        </foreach>
    </insert>
    <select id="top10" parameterType="java.util.Map" resultType="com.sky.dto.GoodsSalesDTO">
        select od.name ,sum(od.number) as number from order_detail as od,orders as o
        where o.id =od.order_id and o.status = 5
        <if test="localDateTime_begin!=null ">
        and order_time &gt; #{localDateTime_begin}
        </if>
        <if test="localDateTime_end !=null">
        and order_time &lt; #{localDateTime_end}
        </if>
        group by name
        order by number desc
        limit 10
        </select>
</mapper>
